// Generated by LiveScript 1.3.1
var menu, btn, sample, s, names, res$, k, gs, gn, i$, len$, idx, i, n, j$, ref$, node, local;
menu = document.querySelector('.dropdown-menu');
btn = document.querySelector('.dropdown .btn');
sample = menu.childNodes[0].cloneNode(true);
s = menu.childNodes[0];
s.parentNode.removeChild(s);
res$ = [];
for (k in anikit.types) {
  res$.push(k);
}
names = res$;
gs = [[], [], [], []];
gn = ["No Animation", "Popular Animation", "Repeat Animation", "Transition"];
for (i$ = 0, len$ = names.length; i$ < len$; ++i$) {
  k = names[i$];
  idx = k === 'static'
    ? 0
    : /\-(on|off|in|out)$/.exec(k) ? 3 : 2;
  gs[idx].push(k);
  if (k === 'bounce' || k === 'bounce-in' || k === 'bounce-out' || k === 'fade' || k === 'blink' || k === 'tremble' || k === 'spin' || k === 'pulse') {
    gs[1].push(k);
  }
}
for (i$ = 0; i$ < 4; ++i$) {
  i = i$;
  n = document.createElement("h6");
  n.classList.add('dropdown-header');
  n.innerText = gn[i];
  menu.appendChild(n);
  for (j$ = 0, len$ = (ref$ = gs[i]).length; j$ < len$; ++j$) {
    k = ref$[j$];
    node = sample.cloneNode(true);
    node.querySelector('span').innerText = k;
    node.querySelector('.ld').classList.add("ld-" + k);
    node.setAttribute('data-name', k);
    menu.appendChild(node);
  }
  if (i < 3) {
    n = document.createElement("div");
    n.classList.add('dropdown-divider');
    menu.appendChild(n);
  }
}
local = {};
menu.addEventListener('click', function(e){
  var that;
  if (e.target.classList.contains('dropdown-item')) {
    if (that = local.active) {
      that.classList.remove('active');
    }
    e.target.classList.add('active');
    local.active = e.target;
    console.log(e.target);
    return btn.innerText = e.target.getAttribute('data-name');
  }
});