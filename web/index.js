// Generated by LiveScript 1.3.1
var menu, btn, s, sample, tgt, names, res$, k, gs, gn, i$, len$, idx, local, ldcv, popupit, popup, i, g, n, ref$, j$, node;
menu = document.querySelector('.dropdown-menu .in');
btn = document.querySelector('.dropdown .btn');
s = document.querySelector('.dropdown-menu .dropdown-item');
sample = s.cloneNode(true);
s.parentNode.removeChild(s);
tgt = document.querySelector('.tgt');
res$ = [];
for (k in anikit.types) {
  res$.push(k);
}
names = res$;
gs = [[], [], [], []];
gn = ["No Animation", "Popular Animation", "Repeat Animation", "Transition"];
for (i$ = 0, len$ = names.length; i$ < len$; ++i$) {
  k = names[i$];
  idx = k === 'static'
    ? 0
    : /\-(on|off|in|out)$/.exec(k) ? 3 : 2;
  gs[idx].push(k);
  if (k === 'bounce' || k === 'bounce-in' || k === 'bounce-out' || k === 'fade' || k === 'blink' || k === 'tremble' || k === 'spin' || k === 'pulse') {
    gs[1].push(k);
  }
}
local = {};
ldcv = new ldCover({
  root: '.ldcv'
});
popupit = function(){
  return ldcv.get().then(function(it){
    return tgt.className = "tgt ld ld-" + it;
  });
};
popup = document.querySelector('.ldcv .base .inner');
local.count = 0;
for (i$ = 0; i$ < 4; ++i$) {
  i = i$;
  g = document.createElement("div");
  if (i > 0) {
    popup.appendChild(document.createElement("hr"));
  }
  n = document.createElement("h6");
  ref$ = n.style;
  ref$.margin = "20px";
  ref$.color = '#444';
  n.innerText = gn[i];
  popup.appendChild(n);
  g.classList.add('lditems');
  popup.appendChild(g);
  for (j$ = 0, len$ = (ref$ = gs[i]).length; j$ < len$; ++j$) {
    k = ref$[j$];
    node = document.createElement("div");
    node.setAttribute('data-ldcv-set', k);
    node.innerHTML = "<div class=\"ldo\"><div class=\"ldin\"><div class=\"ld ld-" + k + "\"></div></div></div>\n<div class=\"name\">" + k + "</div>";
    node.classList.add('lditem');
    if (local.count > 10) {
      node.classList.add('disabled');
    }
    g.appendChild(node);
    local.count = (local.count || 0) + 1;
  }
}
local.count = 0;
for (i$ = 0; i$ < 4; ++i$) {
  i = i$;
  n = document.createElement("h6");
  n.classList.add('dropdown-header');
  n.innerText = gn[i];
  menu.appendChild(n);
  for (j$ = 0, len$ = (ref$ = gs[i]).length; j$ < len$; ++j$) {
    k = ref$[j$];
    node = sample.cloneNode(true);
    node.querySelector('span').innerText = k;
    node.querySelector('.ld').classList.add("ld-" + k);
    node.setAttribute('data-name', k);
    menu.appendChild(node);
    if (local.count > 10) {
      node.classList.add('disabled');
    }
    local.count = (local.count || 0) + 1;
  }
  if (i < 3) {
    n = document.createElement("div");
    n.classList.add('dropdown-divider');
    menu.appendChild(n);
  }
}
menu.addEventListener('click', function(e){
  var that;
  if (e.target.classList.contains('dropdown-item')) {
    if (that = local.active) {
      that.classList.remove('active');
    }
    e.target.classList.add('active');
    local.active = e.target;
    console.log(e.target);
    return btn.innerText = e.target.getAttribute('data-name');
  }
});